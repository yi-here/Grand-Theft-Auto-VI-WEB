// Rendering quality, read by mesh builders so shadow casting is set at
// construction time. `?low` in the URL turns off the expensive extras.

export const quality = {
  shadows: true,
  bloom: true,
};
